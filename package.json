{
  "name": "page-pulse",
  "version": "1.0.0",
  "description": "Audits a URL and returns HTTP status, response time, title, meta description, H1 count, image alt-text coverage, and approximate word count.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "test": "jest"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
